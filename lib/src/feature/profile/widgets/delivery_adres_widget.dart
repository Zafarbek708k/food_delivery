import 'package:flutter/material.dart';

class DeliveryAdresWidget extends StatelessWidget {
  const DeliveryAdresWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('No data',
            style: TextStyle(
              color: Colors.black26,
              fontSize: 20,
            )

        ),
      ),
    );
  }
}
